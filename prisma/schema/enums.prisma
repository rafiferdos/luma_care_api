enum UserRole {
  SUPER_ADMIN
  ADMIN
  DOCTOR
  PATIENT
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum AuthProvider {
  CREDENTIALS
  GOOGLE
}

enum UserGender {
  MALE
  FEMALE
  OTHER
}
